# Copia este archivo como .env (NUNCA subas .env real al repo) para pruebas locales.
# En GitHub Actions, esta misma clave se configura como Secret del repositorio
# (Settings > Secrets and variables > Actions), no en este archivo.

ANTHROPIC_API_KEY=sk-ant-xxxxxxxx
